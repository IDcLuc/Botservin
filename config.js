require('dotenv').config()

module.exports = {
    username: "Botservin",
    prefix: "!",
    password: process.env.password,
    channels: ['observin_'],
    owners:  ['idcluc', 'idcluck', 'observin_']
}